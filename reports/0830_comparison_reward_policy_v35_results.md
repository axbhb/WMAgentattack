# v35 comparison reward results

Status: pending frozen remote execution.

The protocol and implementation were frozen locally before reading the complete
400-configuration outcome archive. Remote execution is blocked until the NTU
server account password is renewed interactively.
